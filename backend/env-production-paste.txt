# ========== Backend .env برای سرور (kaya.fxguard.io) ==========
# محتوا را کپی کن و در سرور داخل فایل backend/.env بچسبان (جایگزین کل فایل کن)

# ---------- الزامی ----------
USE_SQLITE=true
PORT=3002
NODE_ENV=production

# کلید JWT — در production حتماً یک رشتهٔ طولانی و تصادفی بگذار
JWT_SECRET=YourSuperSecretJWTKey123456789!

# آدرس فرانت و CORS (دامنهٔ واقعی پنل)
CORS_ORIGINS=https://kaya.fxguard.io,http://kaya.fxguard.io
FRONTEND_URL=https://kaya.fxguard.io

# آدرس عمومی بک‌اند — برای لینک رسانه/آپلود و ارسال به واتساپ الزامی است
BACKEND_PUBLIC_URL=https://kaya.fxguard.io

# ---------- Gateway واتساپ ----------
# روی همین سرور؛ localhost درسته
GATEWAY_URL=http://127.0.0.1:3001
GATEWAY_API_SECRET=fb9c511e6019a3d7f599f000cf40352d44cd0cdb5943c2d922c780ef03a8590b

# ---------- ایمیل (SMTP) — اختیاری ----------
# اگر پر نکنید، می‌توانید از «تنظیمات پنل» > «تنظیمات ایمیل (SMTP)» در خود پنل استفاده کنید.
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASS=your-password
# SMTP_FROM=noreply@yourdomain.com
# SMTP_FROM_NAME=پورتال کارکنان
# SMTP_SECURE=false
# EMAIL_LOGIN_NOTIFICATION=false

# ---------- مسیریابی هوشمند (اختیاری) ----------
# پیام‌های واتساپ بر اساس فهم معنایی به دپارتمان و کارمند مناسب وصل می‌شوند.
# بدون این کلید: تطابق کلمات کلیدی + مترادف (کافی برای اکثر موارد)
# با این کلید: فهم عمیق‌تر با AI برای پیام‌های مبهم
# OPENAI_API_KEY=sk-...

# ---------- Gateway — اگر خطای «الان نمی‌شود دستگاه وصل شود» زیاد می‌آید ----------
# WHATSAPP_RECONNECT_DELAY_MS=90000
