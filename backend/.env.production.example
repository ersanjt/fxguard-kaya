# نمونه تنظیمات برای سرور (Production)
# این فایل را کپی کنید به .env و مقادیر را پر کنید

NODE_ENV=production
PORT=3002

# با true فقط SQLite استفاده می‌شود (ساده؛ بدون PostgreSQL)
USE_SQLITE=true

# کلید JWT — حتماً یک رشته طولانی و تصادفی بگذارید
JWT_SECRET=change-this-to-a-long-random-string-in-production

# آدرس‌های مجاز برای CORS (با کاما جدا) — حتماً تمام دامنه‌ها و ساب‌دامین‌ها را اضافه کنید
# Socket.IO هم از همین لیست استفاده می‌کند
# CORS_ORIGINS=https://kaya.fxguard.io,https://app.fxguard.io,https://fxguard.io,https://www.fxguard.io
CORS_ORIGINS=https://kaya.fxguard.io,https://app.fxguard.io,https://fxguard.io,https://www.fxguard.io
FRONTEND_URL=https://kaya.fxguard.io
# لینک صفحه فروش پلن‌ها
# SALES_URL=https://fxguard.io

# حالت دمو (اختیاری)
# DEMO_MODE=true
# DEMO_USERNAME=demo
# DEMO_PASSWORD=123456
# DEMO_EMAIL=demo@fxguard.io
# FXGUARD_PUBLIC_SITE=true   # فقط روی app.fxguard.io (برند کایا از API پنل نمی‌آید)

# آدرس Gateway واتساپ (اگر روی همین سرور است)
GATEWAY_URL=http://127.0.0.1:3001

# اختیاری: PostgreSQL (اگر USE_SQLITE=false)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=whatsapp_crm
# DB_USER=crm_user
# DB_PASSWORD=your_secure_password

# اختیاری: Redis
# REDIS_URL=redis://localhost:6379
