# دیتابیس: با true فقط SQLite استفاده می‌شود (بدون PostgreSQL)
USE_SQLITE=true
PORT=3002
NODE_ENV=development
# کلید JWT — در production حتماً عوض کنید
JWT_SECRET=YourSuperSecretJWTKey123456789!
# ادمین اصلی — ایمیل و رمز (در production حتماً عوض کنید)
# MAIN_ADMIN_EMAIL=admin@kaya.local
# MAIN_ADMIN_PASSWORD=your-secure-password
# مبداهای مجاز CORS (با کاما جدا کنید). برای فرم تماس لندینگ fxguard.io را اضافه کنید.
# CORS_ORIGINS=http://localhost:3000,http://localhost:3002,https://fxguard.io,https://www.fxguard.io
CORS_ORIGINS=http://localhost:3000,http://localhost:3002
FRONTEND_URL=http://localhost:3000
# آدرس Gateway واتساپ (برای QR و ارسال پیام)
GATEWAY_URL=http://localhost:3001
# کلید امنیتی — باید با GATEWAY_API_SECRET در Gateway یکسان باشد (حداقل ۳۲ کاراکتر)
# تولید: openssl rand -hex 32
# GATEWAY_API_SECRET=
# پشتیبانی ورود: ایمیل یا URL لینک تماس (برای صفحه ورود)
# SUPPORT_EMAIL=support@fxguard.io
# SUPPORT_URL=https://wa.me/905010676486

# دامنه‌هایی که / به /dashboard ریدایرکت می‌شوند (با کاما جدا). app.fxguard.io = فرانت/لندینگ فقط
# REDIRECT_ROOT_TO_DASHBOARD_HOSTS=kaya.fxguard.io

# SMTP برای fxguard.io — راهنما: backend/docs/EMAIL-SETUP-FXGUARD.md
# cPanel: SMTP_HOST=mail.fxguard.io | GoDaddy Email: SMTP_HOST=smtpout.secureserver.net
# SMTP_HOST=mail.fxguard.io
# SMTP_PORT=587
# SMTP_USER=noreply@fxguard.io
# SMTP_PASS=
# SMTP_FROM=noreply@fxguard.io
# SMTP_FROM_NAME=پورتال کارکنان
# SMTP_SECURE=false
# EMAIL_LOGIN_NOTIFICATION=false
# ایمیل دریافت فرم تماس لندینگ (پیش‌فرض: sales@fxguard.io)
# CONTACT_EMAIL=sales@fxguard.io

# OpenAI — برای مسیریابی هوشمند به دپارتمان و پاسخ خودکار هوش مصنوعی
# دریافت کلید از: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
AI_ANSWER_ENABLED=true
# مدل (اختیاری): gpt-4o-mini | gpt-4o | ...
# OPENAI_MODEL=gpt-4o-mini
