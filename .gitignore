# node
node_modules/
backend/node_modules/
gateway/node_modules/
frontend/node_modules/

# Vite dashboard (ESM) — خروجی بیلد؛ با npm run dashboard:build تولید می‌شود
backend/public/js/app/

# env
.env
.env.local
.env.*.local

# logs
*.log
logs/
backend/logs/

# sqlite
*.sqlite
*.sqlite-shm
*.sqlite-wal
backend/database.sqlite

# backups
backups/

# uploads/runtime data
backend/uploads/
gateway/uploads/

# whatsapp-web.js cache/auth
gateway/.wwebjs_auth/
gateway/.wwebjs_cache/
gateway/sessions/
gateway/sessions*/
gateway/.wwebjs_auth*/
gateway/.wwebjs_cache*/

backend/.env
backend/env-production-paste.txt

# mobile build artifacts — سورس android-app / ios-app / mobile-shared داخل مخزن است
android-app/.gradle/
android-app/build/
android-app/app/build/
android-app/local.properties
ios-app/**/xcuserdata/
ios-app/**/DerivedData/
ios-app/**/*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# local backups / temp
*.bak.*
*.prebind.*
*.trace

# env backups
*.env.bak.*

# runtime uploads
gateway/src/uploads/

# CI: staged APK for SCP (runner-only; ignore if created locally)
deploy-artifacts/
/app-release.apk
