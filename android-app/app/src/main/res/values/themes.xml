<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.KayaCRM" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorPrimary">#075E54</item>
        <item name="android:colorPrimaryDark">#054D45</item>
    </style>

    <!-- Splash رسمی (Android 12+) با سازگاری عقب‌گرد از androidx.core:core-splashscreen -->
    <style name="Theme.KayaCRM.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.KayaCRM</item>
    </style>
</resources>
