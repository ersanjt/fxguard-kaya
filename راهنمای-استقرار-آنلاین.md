# راهنمای استقرار آنلاین (دیتابیس + بک‌اند)

## چرا روی Vercel نمی‌شود؟

**Vercel** برای سایت‌های استاتیک و **توابع سرورلس** (هر درخواست جدا، حداکثر چند ثانیه) است. این پروژه:

- یک **سرور Express** همیشه روشن است (نه تابع سرورلس).
- **Socket.io** و اتصال دائمی واتساپ (Gateway) نیاز به پروسس همیشه فعال دارد.
- روی Vercel نمی‌توان سرور را ۲۴ ساعته روشن نگه داشت و دیتابیس هم سرویس جدا است.

پس **کل پروژه را نمی‌توان فقط روی Vercel آنلاین کرد**. دیتابیس را هم Vercel به‌تنهایی «نمی‌سازد»؛ باید از یک سرویس دیتابیس (مثل Railway یا Neon) استفاده کنید.

---

## راه‌حل پیشنهادی: Railway (یا Render)

با **Railway** یا **Render** می‌توانید هم **بک‌اند** را اجرا کنید هم **دیتابیس PostgreSQL** را همان‌جا بسازید و همه‌چیز آنلاین شود.

### گام‌های استقرار روی Railway

1. **ثبت‌نام و ساخت پروژه**
   - برو به [railway.app](https://railway.app) و با GitHub لاگین کن.
   - **New Project** بزن.

2. **اضافه کردن PostgreSQL**
   - در پروژه روی **+ New** کلیک کن.
   - **Database** → **PostgreSQL** را انتخاب کن.
   - بعد از ساخته شدن، وارد سرویس PostgreSQL شو، تب **Variables** یا **Connect**.
   - متغیر **`DATABASE_URL`** را کپی کن (مثل  
     `postgresql://postgres:xxxx@xxx.railway.app:5432/railway`).

3. **اضافه کردن سرویس بک‌اند (Node)**
   - دوباره **+ New** → **GitHub Repo** و ریپوی همین پروژه را انتخاب کن.
   - **Root Directory** را روی پوشه‌ی **`backend`** بگذار (چون `package.json` و `server.js` داخل `backend` است).
   - **Build Command:** `npm install`
   - **Start Command:** `npm start` (یا `node server.js`)
   - **Settings** → متغیرهای محیط (Environment Variables) را اضافه کن:
     - `DATABASE_URL` = همان مقدار کپی‌شده از PostgreSQL
     - `JWT_SECRET` = یک رشته تصادفی طولانی (مثلاً با یک مولد آنلاین)
     - `PORT` = معمولاً Railway خودش `PORT` را ست می‌کند؛ اگر خواستی دستی بزن: `3002`
     - برای ساده‌تر شدن در شروع:
       - `USE_SQLITE` را نگذار یا `false` بگذار تا از همان PostgreSQL استفاده شود.
       - اگر Redis / RabbitMQ / MongoDB نداری، سرور با خطای اتصال آن‌ها به‌صورت اختیاری ادامه می‌دهد (لاگ هشدار می‌دهد ولی بالا می‌آید).

4. **دامین و آدرس آنلاین**
   - در سرویس بک‌اند، **Settings** → **Networking** → **Generate Domain**.
   - آدرسی مثل `https://xxxx.up.railway.app` می‌گیری.

5. **داشبورد و API**
   - داشبورد داخل همین بک‌اند سرو می‌شود؛ پس با همان آدرس Railway می‌توانی بروی:
     - مثلاً: `https://xxxx.up.railway.app/dashboard.html`
   - ورود پیش‌فرض: `admin@company.com` / `Admin@123`

6. **اتصال واتساپ (Gateway)**
   - Gateway در این ریپو یک پروسس جدا است (معمولاً در پوشه‌ی `gateway`). برای کار ۲۴ ساعته واتساپ باید آن را یا روی همان سرور (مثلاً یک سرویس دوم روی Railway) یا روی یک VPS جدا اجرا کنی و در تنظیمات بک‌اند آدرس آن را بگذاری. بدون Gateway فقط پنل و API کار می‌کند؛ دریافت/ارسال واقعی واتساپ از همان Gateway انجام می‌شود.

---

## دیتابیس «همان جا»

- در Railway (یا Render) دیتابیس را **همان داخل پروژه** می‌سازی (PostgreSQL).
- با ست کردن **`DATABASE_URL`** در بک‌اند، کد به‌صورت خودکار از همین دیتابیس استفاده می‌کند و نیازی به نصب جداگانه دیتابیس روی کامپیوتر خودت نیست.

---

## خلاصه

| مورد | Vercel | Railway / Render |
|------|--------|-------------------|
| سرور همیشه روشن | ❌ | ✅ |
| دیتابیس همان‌جا | ❌ (فقط لینک به DB خارجی) | ✅ (PostgreSQL در همان پروژه) |
| داشبورد + API | فقط اگر بک‌اند جای دیگر باشد | ✅ با یک دامنه |
| Gateway واتساپ | ❌ | باید جدا (همان سرور یا VPS) |

برای **آنلاین کردن کل سیستم و دیتابیس همان‌جا**، از **Railway** (یا Render) برای بک‌اند + PostgreSQL استفاده کن؛ نه فقط Vercel.

اگر خواستی برای **Render** هم گام‌به‌گام بنویسم (مثل Railway)، بگو تا همان را هم اضافه کنم.
