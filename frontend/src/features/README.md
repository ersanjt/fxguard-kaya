# features/

هر زیرپوشه یک **قابلیت دامنه‌ای** (مکالمات، مشتریان، تیکت، …).

- ورودی پیشنهادی: `initXxx(appContext)` از `main.js` یا لود تنبل.
- وابستگی فقط به `@core` و `@shared` — نه import متقابل بین featureها.
