# به‌روزرسانی سیستم - نسخه قابل استفاده

## تغییرات انجام‌شده

### ۱. ارتباط Gateway و Backend بدون RabbitMQ
- **Gateway:** اگر RabbitMQ نباشد، پیام‌های دریافتی واتساپ با **HTTP** به آدرس `POST /api/webhook/incoming-message` روی Backend فرستاده می‌شوند.
- **Backend:** اگر RabbitMQ نباشد، برای ارسال پیام به واتساپ از **HTTP** به آدرس Gateway یعنی `POST /api/send-message` استفاده می‌شود.
- نتیجه: حتی بدون نصب RabbitMQ، جریان «دریافت پیام در Gateway → ذخیره در Backend» و «ارسال پیام از Backend → ارسال در واتساپ» کار می‌کند.

### ۲. وب‌هوک ورودی
- **Backend:** مسیر `POST /api/webhook/incoming-message` اضافه شد تا Gateway بتواند پیام ورودی را به Backend بفرستد.
- پردازش پیام (ساخت/به‌روز مشتری، مکالمه، ذخیره پیام، پاسخ خودکار، تخصیص) مثل قبل انجام می‌شود.

### ۳. ارسال پیام از داشبورد
- **Backend:** مسیر `POST /api/conversations/:id/send` با احراز هویت اضافه شد.
- با ارسال توکن و متن پیام، پیام در دیتابیس ذخیره و از طریق Gateway به واتساپ فرستاده می‌شود.

### ۴. داشبورد کامل (CRM)
- **ورود:** ذخیره توکن در مرورگر و نمایش بخش اصلی بعد از لاگین.
- **مکالمات:** لیست مکالمات، کلیک روی هر کدام برای باز کردن چت و دیدن پیام‌ها.
- **ارسال پیام:** نوشتن متن و زدن ارسال (یا Enter) برای فرستادن به واتساپ.
- **مشتریان:** لیست مشتریان.
- **دپارتمان‌ها:** لیست دپارتمان‌ها.
- **خروج:** پاک شدن توکن و بازگشت به صفحه ورود.

### ۵. امنیت API
- برای دسترسی به مکالمات، مشتریان، دپارتمان‌ها، ارسال پیام و غیره، ارسال **توکن (Bearer)** در هدر درخواست الزامی است.

### ۶. اصلاحات جزئی
- نرمال‌سازی شماره تلفن در پردازش پیام ورودی (حذف `@c.us` در صورت وجود).
- پشتیبانی از timestamp به صورت ثانیه یا میلی‌ثانیه.
- جلوگیری از خطا وقتی `body` یا `contact` خالی است.

---

## نحوه استفاده

1. **Backend:** `cd backend` و `node server.js`
2. **Gateway:** `cd gateway` و `node src/index.js` و اسکن QR با واتساپ
3. **داشبورد:** در مرورگر باز کنید: **http://localhost:3002/dashboard.html**
4. ورود با `admin@company.com` / `Admin@123`
5. از تب مکالمات، یک مکالمه را باز کنید و پیام بفرستید.

بدون نیاز به RabbitMQ، Redis یا PostgreSQL (با USE_SQLITE=true) سیستم قابل استفاده است.
