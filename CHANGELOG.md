# به‌روزرسانی سیستم - نسخه قابل استفاده

## تغییرات انجام‌شده

### بهبود بخش مکالمات (فوریه ۲۰۲۵)
- **وب داشبورد:** رفع ایموجی‌های خراب در حالت خالی پیام‌ها و تاریخچه؛ اصلاح آواتار در modal مکالمه جدید؛ بهبود استایل لیست مکالمات و آواتار مشتریان.
- **اندروید:** افزودن metadata برای پشتیبانی گروه‌ها؛ بهبود UI لیست مکالمات (آواتار، زمان، نام گروه/مشتری)؛ null safety برای پاسخ API؛ key برای LazyColumn جهت recomposition بهتر.
- **iOS:** افزودن metadata و displayName برای گروه‌ها؛ بهبود ConversationRow با آواتار و زمان آخرین پیام.

### رفع نمایش فرستنده در چت‌های گروهی
- **Gateway:** استخراج فرستنده پیام با چند fallback (`msg.author`, `msg._data.participant`, `msg._data.key.participant`, `msg.id.participant`) برای سازگاری با نسخه‌های مختلف whatsapp-web.js.
- **Dashboard:** بهبود نمایش نام/شماره فرستنده برای پیام‌های ورودی گروه؛ پشتیبانی از فرمت‌های مختلف `senderId` (مثل `@c.us`, `@s.whatsapp.net`).
- نتیجه: در بخش مکالمات، برای هر پیام گروهی مشخص می‌شود چه کسی آن را ارسال کرده است.

### نمایش IP و کشور در ورودها و وضعیت آنلاین
- **ورودها:** در بخش «آخرین ورودها»، ستون‌های **IP** و **کشور** اضافه شد تا آدرس IP و کشور اتصال هر ورود نمایش داده شود.
- **کارکنان آنلاین:** در جدول «کارکنان آنلاین»، IP و کشور آخرین ورود هر کاربر نمایش داده می‌شود.
- **ذخیره‌سازی:** هنگام ورود، IP و کشور (از geoip-lite) در metadata لاگ فعالیت ذخیره می‌شود.

### رفع خطای ارسال پیام صوتی (ویس) در چت واتساپ
- **Gateway:** قبلاً URLهای `localhost` برای رسانه رد می‌شدند (امنیت SSRF). در محیط توسعه یا با `MEDIA_ALLOW_LOCALHOST=true`، ارسال ویس و فایل از backend لوکال اکنون مجاز است.

### پشتیبانی از چت‌های گروهی واتساپ (جدید)
- **Gateway:** ارسال `author` (فرستنده) برای پیام‌های گروهی؛ API جدید `GET /api/chats/groups` برای دریافت لیست گروه‌ها.
- **Backend:** پردازش پیام‌های گروهی (قبلاً نادیده گرفته می‌شدند). هر گروه به‌صورت یک مکالمه با نام گروه نمایش داده می‌شود. پاسخ خودکار و AI فقط برای چت مستقیم فعال است.
- **همگام‌سازی گروه‌ها:** دکمه «همگام‌سازی گروه‌ها» در صفحه مکالمات — همه گروه‌های واتساپ را در CRM نمایش می‌دهد.
- **ارسال به گروه:** امکان پاسخ و ارسال پیام به گروه‌ها از پنل.
- **بهبودهای گروه:** تب «گروه‌ها» برای فیلتر؛ نمایش نام فرستنده در پیام‌های گروهی؛ نشان گروه در هدر چت؛ UX بهتر برای همگام‌سازی (loading، خطای اتصال).

### ۱. ارتباط Gateway و Backend بدون RabbitMQ
- **Gateway:** اگر RabbitMQ نباشد، پیام‌های دریافتی واتساپ با **HTTP** به آدرس `POST /api/webhook/incoming-message` روی Backend فرستاده می‌شوند.
- **Backend:** اگر RabbitMQ نباشد، برای ارسال پیام به واتساپ از **HTTP** به آدرس Gateway یعنی `POST /api/send-message` استفاده می‌شود.
- نتیجه: حتی بدون نصب RabbitMQ، جریان «دریافت پیام در Gateway → ذخیره در Backend» و «ارسال پیام از Backend → ارسال در واتساپ» کار می‌کند.

### ۲. وب‌هوک ورودی
- **Backend:** مسیر `POST /api/webhook/incoming-message` اضافه شد تا Gateway بتواند پیام ورودی را به Backend بفرستد.
- پردازش پیام (ساخت/به‌روز مشتری، مکالمه، ذخیره پیام، پاسخ خودکار، تخصیص) مثل قبل انجام می‌شود.

### ۳. ارسال پیام از داشبورد
- **Backend:** مسیر `POST /api/conversations/:id/send` با احراز هویت اضافه شد.
- با ارسال توکن و متن پیام، پیام در دیتابیس ذخیره و از طریق Gateway به واتساپ فرستاده می‌شود.

### ۴. داشبورد کامل (CRM)
- **ورود:** ذخیره توکن در مرورگر و نمایش بخش اصلی بعد از لاگین.
- **مکالمات:** لیست مکالمات، کلیک روی هر کدام برای باز کردن چت و دیدن پیام‌ها.
- **ارسال پیام:** نوشتن متن و زدن ارسال (یا Enter) برای فرستادن به واتساپ.
- **مشتریان:** لیست مشتریان.
- **دپارتمان‌ها:** لیست دپارتمان‌ها.
- **خروج:** پاک شدن توکن و بازگشت به صفحه ورود.

### ۵. امنیت API
- برای دسترسی به مکالمات، مشتریان، دپارتمان‌ها، ارسال پیام و غیره، ارسال **توکن (Bearer)** در هدر درخواست الزامی است.

### ۶. اصلاحات جزئی
- نرمال‌سازی شماره تلفن در پردازش پیام ورودی (حذف `@c.us` در صورت وجود).
- پشتیبانی از timestamp به صورت ثانیه یا میلی‌ثانیه.
- جلوگیری از خطا وقتی `body` یا `contact` خالی است.

---

## نحوه استفاده

1. **Backend:** `cd backend` و `node server.js`
2. **Gateway:** `cd gateway` و `node src/index.js` و اسکن QR با واتساپ
3. **داشبورد:** در مرورگر باز کنید: **http://localhost:3002/dashboard.html**
4. ورود با `admin@kaya.fxguard.io` / `Admin@123`
5. از تب مکالمات، یک مکالمه را باز کنید و پیام بفرستید.

بدون نیاز به RabbitMQ، Redis یا PostgreSQL (با USE_SQLITE=true) سیستم قابل استفاده است.
