# راه‌اندازی سریع (بدون Docker، فقط SQLite)

## ۱. پیش‌نیاز
- **Node.js** نسخه ۱۸ یا بالاتر ([دانلود](https://nodejs.org))

## ۲. اجرای Backend و داشبورد

**ویندوز (PowerShell):**
```powershell
.\start-backend.ps1
```

**یا به صورت دستی:**
```bash
cd backend
npm install
# اگر فایل .env ندارید: کپی از .env.example
node server.js
```

پس از بالا آمدن سرور، در مرورگر باز کنید:
**http://localhost:3002/dashboard.html**

## ۳. ورود به داشبورد
- **ایمیل:** `admin@company.com`
- **رمز:** `Admin@123`

(در اولین اجرا، در صورت نبودن کاربر ادمین، به‌صورت خودکار ساخته می‌شود.)

## ۴. اتصال واتساپ (اختیاری)
1. از منوی سمت راست روی **«اتصال واتساپ»** کلیک کنید.
2. روی **«شروع Gateway واتساپ»** کلیک کنید (یک بار کافی است).
3. پس از چند ثانیه، QR نمایش داده می‌شود؛ آن را با اپ واتساپ موبایل اسکن کنید.

برای اجرای دستی Gateway در یک ترمینال جدا:
```bash
cd gateway
npm install
node src/index.js
```

---

**عیب‌یابی:** اگر پیام «سرور Backend پاسخ درست نمی‌دهد» دیدید، مطمئن شوید دستور `node server.js` از داخل پوشه `backend` اجرا شده و پورت ۳۰۰۲ بسته نیست.
